#include <stdio.h>

int main() {
    int a = 5, b = 10;
    int sum = a + b;
    printf("Sum is: %d\n", sum);

    // This is a single-line comment
    /*
       This is a multi-line comment
    */

    if (sum > 10) {
        sum++;
    }
    return 0;
}
